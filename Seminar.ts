export class Seminar {
    constructor(public mandant: number, public ident_nr: number, public titel:string, public typ:string, public thema: string, public beschr: string, public tage: number, public adressat: string, public an_part: string, public status: string

    ) {}
}